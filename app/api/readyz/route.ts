import { upstreamUrl, VERSION } from "@/config/env"; import { fetchMarketData } from "@/clients/market-data-client"; import { tools } from "@/tools/all";
export const runtime="nodejs"; export const dynamic="force-dynamic"; export const maxDuration=10;
export async function GET(){ try{ new URL(upstreamUrl()); const r=await fetchMarketData(60000); return Response.json({status:"ready",version:VERSION,toolCount:tools.length,upstreamReachable:true,upstreamLatencyMs:r.meta.durationMs,upstreamSchemaType:r.payload.btcIntraday?"enriched":"legacy",cacheStatus:r.meta.cacheStatus},{headers:{"cache-control":"no-store"}});}catch{ return Response.json({status:"not_ready",version:VERSION,toolCount:tools.length,upstreamReachable:false},{status:503,headers:{"cache-control":"no-store"}});} }
