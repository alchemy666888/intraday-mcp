# Documentation

This project is a read-only BTC Intraday Market Data MCP for Vercel Hobby-compatible ChatGPT use. It exposes public market data only, validates input and upstream payloads, preserves venue/source/units/timestamps, uses null for unavailable numeric values, and never fabricates missing metrics or provides trade execution instructions.
